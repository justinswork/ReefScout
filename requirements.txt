# Pinned to the versions ReefScout was built and tested against (Python 3.10–3.11),
# for reproducible deploys. Regenerate with `pip freeze` after intentional upgrades.

# Web backend
fastapi==0.136.3
uvicorn[standard]==0.49.0

# LLM + MCP
anthropic==0.109.1
mcp==1.27.2

# HTTP client for the live ocean APIs
httpx==0.28.1

# Config / secrets
python-dotenv==1.2.2

# Testing / eval (used by pytest + eval/run_eval.py)
pytest==9.0.3
pytest-asyncio==1.4.0
